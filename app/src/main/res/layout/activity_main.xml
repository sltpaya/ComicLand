<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:orientation="vertical"
    tools:context="org.sltpaya.comiclands.activity.MainActivity">

    <FrameLayout
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/bottom_navigation_height">

    </FrameLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottom_navigation_height"
        android:layout_gravity="bottom"
        android:clipChildren="false"
        app:layout_behavior="@string/bottom_behavior">

        <org.sltpaya.tablayout.TabLayoutBuilder xmlns:app="http://schemas.android.com/apk/res-auto"
            android:id="@+id/main_bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_index_bottom_nav"
            app:tabBackground="@null"
            app:tabIndicatorHeight="0dp">

        </org.sltpaya.tablayout.TabLayoutBuilder>

    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>
